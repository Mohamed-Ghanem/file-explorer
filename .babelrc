{
  "presets": [
    "@babel/preset-env",
    [
      "@babel/preset-react",
      {
        "runtime": "automatic" // Use the new JSX Transform (no need to import React)
      }
    ],
    "@babel/preset-typescript"
  ]
}
